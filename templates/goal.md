---
status: active
round: 0
max_rounds: 5
cost_usd: 0
created: YYYY-MM-DD
spec:
---
# ゴール: <タイトル>

## 完了条件

<!-- goal-gate hook が応答完了のたびにこの項目を Haiku で判定する。
     判定器は会話に出力された内容しか読めない。したがって各条件は
     「観測可能な終了状態」と「それを証明するコマンド」をセットで書くこと。
     frontmatter の意味:
       status:     active | done | stalled | abandoned (active 以外は判定しない)
       round:      hook が判定のたびにインクリメントする。手動で編集しない
       max_rounds: この回数を超えたら status: stalled で自動判定を停止
       cost_usd:   judge (Haiku) の累積コスト。hook が更新する。手動で編集しない
       spec:       取り込み元の仕様ファイル(任意) 例: docs/spec/foo.md -->

- [ ] DC-1: <観測可能な終了状態> — 検証: `<コマンド>` が <期待する結果>
- [ ] DC-2:

## 制約

<!-- 逸脱してはいけないこと。goal-gate は制約違反を検出したら未達として差し戻す。
     ゴールドリフト(目的が少しずつズレて当初と違うものを作り始める)への対策。
     spec から取り込む場合は「## 制約」と「## スコープ > 含まない」が入る。
     制約がない場合も「なし」と明記すること。 -->

- C-1:

## 判定履歴

<!-- goal-gate が判定のたびに追記する(直近5件)。手動で編集しない。
     同じ未達を繰り返さないための外部メモリとして、次ターン以降も参照される。 -->
